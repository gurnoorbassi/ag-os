# Archetype Update Proposals

Draft proposals derived from accepted lessons live here. A proposal never edits an archetype automatically and never grants permission.

Every proposal must cite one accepted lesson, name one active archetype and one allowed target section, pass validation, and be applied only through a reviewed pull request. Candidate or rejected lessons cannot create proposals.
