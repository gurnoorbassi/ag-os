# AG OS proposals

The bounded proposal engine writes owner-reviewable next-work suggestions here.

Proposals never execute themselves, never grant permission, and never satisfy a downstream approval gate. Accepting one creates a normal owner command that must pass the same planning, budget, validation, connector, and approval controls as a command typed by the owner.
