# Production Readiness

Fail-closed production-readiness records live here. A record documents safeguards and blockers; it never authorizes production by itself.

`activationAllowed` may become true only when every required check passes with source-controlled evidence and an exact active approval lock covers the production action. Credentials, customer/production data, DNS, paid actions, posting, and deployment remain separately gated.
