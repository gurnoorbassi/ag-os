# Projects

Project records live here.

Every active owner project defines scope, out-of-scope boundaries, trust level, risks, approvals, quality gates, security review, costs, deployment plan, and an `ownerWorkspace` with real live access and source-control status.

Only records referenced by `registry.json` appear as owner projects. Other JSON records in this directory are retained historical evidence and must not be presented as selectable projects.
